name: task-9-env-var-ci
on: [push]
jobs:
  env:
    runs-on: ubuntu-latest
    env:
      APP: medimesh
      TYPE: production
      PORT: 8080
    steps:
      - run: echo "$APP of type $TYPE runs on port $PORT"